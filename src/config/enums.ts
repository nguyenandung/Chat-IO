export enum RoleCode {
  USER = 1,
  ADMIN = 2,
}

export enum SocialProfilePlatform {
  WEBSITE = 1,
  GITHUB = 2,
  LINKEDIN = 3,
  FACEBOOK = 4,
  TIKTOK = 5,
  YOUTUBE = 6,
}

export enum PermissionCode {
  READ = 1,
  WRITE = 2,
  DELETE = 3,
  EXECUTE = 4,
}
